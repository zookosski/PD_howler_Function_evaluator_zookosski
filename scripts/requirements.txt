# PD Howler AI Bridge - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
opencv-python>=4.7.0
numpy>=1.21.0
Pillow>=9.0.0

# PyTorch (CPU version - change for GPU if needed)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# AI model dependencies (optional but recommended)
transformers>=4.30.0
rembg>=2.0.50

# Optional: GPU support (uncomment if you have NVIDIA GPU)
# To use GPU, first uninstall CPU torch:
# pip uninstall torch torchvision torchaudio
# Then install GPU version:
# --extra-index-url https://download.pytorch.org/whl/cu118
# torch>=2.0.0+cu118
# torchvision>=0.15.0+cu118
# torchaudio>=2.0.0+cu118

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
